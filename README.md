# clearForm
Clears all inputs in form

### How it works

*.clearForm()*

` 
 var activeForm = $('form');
`

` 
 activeForm.clearForm({
                    exclude_names: [
                        'member_id'
                    ]
                });
`
